#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../volo/bin/volo" "$@"
  ret=$?
else 
  node  "$basedir/../volo/bin/volo" "$@"
  ret=$?
fi
exit $ret
